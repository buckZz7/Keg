"""Keg — house machinery for the compression race.

The gate (fidelity vs the model's own BF16 reference) is deterministic,
ungameable, and involves no LLM. The race metric is SIZE — the smallest
recipe that still holds the model's behavior.
"""
